import {create} from 'zustand';


export const useContestStore = create((set,get) => ({
  contest: null,
  isContestGetting: false,

  createContest: async () => {

  },

  latestRating: async () => {

  }

}))